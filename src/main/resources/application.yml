spring:
    data:
        mongodb:
            database: admin
            host: mongo
            port: 27017
            username: root
            password: root
logging:
    level:
        root: warn
jwt:
    private:
        key: classpath:rsa.private.key
    public:
        key: classpath:rsa.public.key